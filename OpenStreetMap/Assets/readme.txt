I used before XmlSerializer, but it not worked with 
First i wrote WPF app using system.xml.linq, but then i realized that unity uses .net framework 2.0 mono and there is no system.xml.linq namespace

I didn't used Dictionary<> class because it not serializable, it may be buggy in unity3d.
