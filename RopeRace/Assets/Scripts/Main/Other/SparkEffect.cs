using System;
using UnityEngine;
using System.Linq;
public class SparkEffect: bs
{
    //void OnCollisionStay(Collision c)
    //{
    //    foreach (var p in c.contacts.Take(3))
    //        _Game.Emit(p.point, 1);
    //}
}