using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class UpForce : bs {
    public Vector3 forceFactor ;
    //public List<bs> triggers = new List<bs>();
    //void OnTriggerEnter(Collider c)
    //{
    //    //Debug.Log("Enter" + c.gameObject.name);
    //    var bs = c.gameObject.GetComponent<bs>();
    //    if (bs != null && !triggers.Contains(bs))
    //        triggers.Add(bs);
    //}
    //void onTriggerExit(Collider c)
    //{
    //    //Debug.Log("Exit" + c.gameObject.name);
    //    var bs = c.gameObject.GetComponent<bs>();
    //    if (bs != null)
    //        triggers.Remove(bs);
    //}
}
